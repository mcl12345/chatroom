{{ form_start(form) }}
<div class="row">
  <div class="col-md-4">{{ form_row(form.contenu) }}</div>
  <div class="col-md-4" style="display:none;">{{ form_row(form.auteur) }}</div>
  <div class="col-md-4" style="display:none;">{{ form_row(form.created_at) }}</div>
</div>
{{ form_rest(form) }}
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
